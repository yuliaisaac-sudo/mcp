import { type ComponentProps } from "react";
export type CircularProgressProps = Omit<ComponentProps<"div">, "children"> & {
    /**
     * Maximum duration to use for simulated upload progress in ms
     * @default 15000
     */
    maxDuration?: number;
    /**
     * Sets the progress to full and resolves the simulated timer
     * @default false
     */
    done?: boolean;
    /**
     * Sets the progress to full and resolves the simulated timer
     * @default false
     */
    progress?: number;
    /** Size of the indicator, in pixels
     * @default 28px
     */
    size?: number | string;
    /** Stroke width of the indicator, in pixels, between 1 and 4
     * @default 2
     */
    strokeWidth?: number;
    /** Color of the active progress track (stroke) */
    trackActiveColor?: string;
    /** Color of the base track (background stroke) */
    trackColor?: string;
};
export declare const CircularProgress: ({ maxDuration, done, className, progress: propProgress, size, strokeWidth, trackActiveColor, trackColor, style, ...restProps }: CircularProgressProps) => import("react/jsx-runtime").JSX.Element;
