import { type ReactNode } from "react";
type ValidDirectiveProps = {
    children?: string;
} & Partial<Record<string, unknown>>;
export interface MarkdownDirective<T extends ValidDirectiveProps = ValidDirectiveProps> {
    directiveName: string;
    render: (props: {
        attrs?: string;
        children?: ReactNode;
    }) => ReactNode;
    (props: T): string;
}
export declare function createMarkdownDirective<T extends ValidDirectiveProps>({ render, name, mode, }: {
    render: (props: T) => ReactNode;
    name: string;
    mode: "inline" | "block";
}): MarkdownDirective<T>;
export declare function reactMarkdownRemarkDirective(): (tree: any) => void;
export {};
