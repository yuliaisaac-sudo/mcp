import { type ReactNode } from "react";
export type AvatarGroupProps = {
    /** Class name passed to the group container */
    className?: string;
    /**
     * Determines stacking layer order
     * @default start
     */
    stack?: "start" | "end";
    /** Size all avatars in the group, in pixels. */
    size?: number;
    children: ReactNode;
};
export declare const AvatarGroup: ({ className, stack, size, children }: AvatarGroupProps) => import("react/jsx-runtime").JSX.Element;
