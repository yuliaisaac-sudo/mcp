import { type ClipboardContent } from "../../lib/copyToClipboard";
import { type ButtonProps } from "./Button";
export type CopyButtonProps = {
    copyValue: string | ClipboardContent | (() => string) | (() => ClipboardContent);
    children?: React.ReactNode | ((props: {
        copied: boolean;
    }) => React.ReactNode);
} & Omit<ButtonProps, "children">;
export declare const CopyButton: ({ children, copyValue, onClick, ...restProps }: CopyButtonProps) => import("react/jsx-runtime").JSX.Element;
